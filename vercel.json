{
  "version": 2,
  "builds": [
    
    {
      "src": "react-client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "/dist"
      }
    }
  ],
  "routes": [
    
    {
      "src": "/(.*)",
      "dest": "/react-client/dist/$1" 
    }
  ],
  "buildCommand": "npm install --force && npm run build "
}
